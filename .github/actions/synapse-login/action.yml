name: "Synapse Login"

description: "Log into Synapse"

inputs:
  username:
    description: "Synapse username"
    required: true

  password:
    description: "Synapse password"
    required: true

runs:
  using: "docker"
  image: "Dockerfile"